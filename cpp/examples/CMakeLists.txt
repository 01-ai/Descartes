INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
INCLUDE_DIRECTORIES(${DESCARTES_INCLUDE})

SET(LIBS descartes gflags hdf5_cpp hdf5)

ADD_EXECUTABLE(builder example.cpp)
TARGET_LINK_LIBRARIES(builder ${LIBS})
